{{!< default}}

<section class="masthead masthead-archive error-page">
    <p class="note archive-kicker">{{statusCode}}</p>
    <h1 class="masthead-title">{{message}}</h1>
    <p class="masthead-desc"><a href="{{@site.url}}">Back to the index</a></p>
</section>
