{
  "name": "blog",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Repo-canonical blog content, synced to Ghost via the Admin API.",
  "scripts": {
    "sync": "node scripts/sync.js",
    "sync:prune": "node scripts/sync.js --prune",
    "theme:check": "gscan theme",
    "theme:deploy": "node scripts/deploy-theme.js"
  },
  "dependencies": {
    "@tryghost/admin-api": "^1.14.0",
    "gray-matter": "^4.0.3",
    "marked": "^12.0.0"
  },
  "devDependencies": {
    "gscan": "^6.4.2"
  }
}
